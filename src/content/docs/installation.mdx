---
title: Installation
---

You can install Annotorious with a package manager like npm, or import it into your HTML page 
using a `<script>` tag. 

## Vanilla JavaScript

To annotate JPEG or PNG images using plain JavaScript, install the standard version of Annotorious.

```bash frame="none"
npm install @annotorious/annotorious
```

To annotate zoomable images in OpenSeadragon, install the OpenSeadragon 
plugin version of Annotorious.

```bash frame="none"
npm install  @annotorious/openseadragon
```

### Script Import

To use the standard version of Annotorious without a bundler, import the 
__script__ and the __stylesheet__ file into the `<head>` of your HTML page.

```html
<head>
  <link 
    rel="stylesheet" 
    href="https://cdn.jsdelivr.net/npm/@annotorious/annotorious@latest/dist/annotorious.css">
  <script 
    src="https://cdn.jsdelivr.net/npm/@annotorious/annotorious@latest/dist/annotorious.js"></script>
</head>
```

To use the OpenSeadragon version without a bundler:

```html
<head>
  <link 
    rel="stylesheet" 
    href="https://cdn.jsdelivr.net/npm/@annotorious/openseadragon@latest/dist/annotorious-openseadragon.css">
  <script 
    src="https://cdn.jsdelivr.net/npm/@annotorious/openseadragon@latest/dist/annotorious-openseadragon.js"></script>
</head>
```

## React

To use Annotorious with React, install the `@annotorious/react` package. Note that the 
package includes bindings for the standard version of Annotorious as well as OpenSeadragon.

```bash frame="none"
npm install  @annotorious/react
```

## Svelte

To use Annotorious with Svelte, install the `@annotorious/svelte` package. Note that the package 
includes bindings for the standard version of Annotorious as well as the OpenSeadragon plugin.

```bash frame="none"
npm install  @annotorious/svelte
```

## Using Annotorious

To learn more about how to use Annotorious with the framework of your choice, 
see the following guides:

- [Annotating images with Vanilla JavaScript](/guides/annotating-images/)
- [Annotating images with React](/guides/annotating-images/)
- [Annotating zoomable images with OpenSeadragon using Vanilla JavaScript](/guides/annotating-images/)
- [Annotating zoomable images with OpenSeadragon using React](/guides/annotating-images/)
- [Annotating zoomable images with OpenSeadragon using Svelte](/guides/annotating-images/)
